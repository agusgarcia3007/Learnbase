{
  "name": "new-lms",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"cyan.bold,magenta.bold\" \"bun run --cwd apps/client dev\" \"bun run --cwd apps/server dev\"",
    "build": "concurrently -n \"client,server\" -c \"cyan.bold,magenta.bold\" \"bun run --cwd apps/client build\" \"bun run --cwd apps/server build\"",
    "build:client": "bun run --cwd apps/client build",
    "build:server": "bun run --cwd apps/server build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
